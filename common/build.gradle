dependencies {
  modImplementation "net.fabricmc:fabric-loader:${rootProject.loader_version}"
}

architectury {
  common()
}

minecraft {
  refmapName = 'immersive_armors.mixin.refmap.json' // remove to make it forge runClient compatible
}

java {
  withSourcesJar()
}
